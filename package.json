{
  "name": "diffpal.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:sync": "node scripts/sync-docs.mjs",
    "docs:dev": "npm run docs:sync && vitepress .",
    "docs:build": "npm run docs:sync && vitepress build .",
    "docs:preview": "vitepress preview .",
    "check": "npm run docs:build && node scripts/check-privacy.mjs"
  },
  "overrides": {
    "vite": "6.4.3"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
